variable "instance_type" {

    default = "t2.micro"# variable default value
    #from terminal command terraform plan -var="instance_type=t2.small"
    # if {}is blank then we can give value from ternminal as well like it shows option 
    #Enter the value
    
  
}